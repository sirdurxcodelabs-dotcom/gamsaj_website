import DynamicClientSection from '../../Common/Client/DynamicClientSection'

const Client = () => <DynamicClientSection variant="dark" centerMode={false} className="home-one-client" />

export default Client
